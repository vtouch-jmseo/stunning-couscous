# from video_writer import VideoWriter
save_prefix : "./record"
record_time_sec : 30
frame_type : 'mp' # set 'mp' or 'qmp'

# Device default setting 
# DO NOT change
ip : "10.42.0.1"
config : "./config/tof-viewer_config.json"

